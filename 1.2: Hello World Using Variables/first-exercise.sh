#/bin/bash # sets the terminal that we use
clear # clears the terminal
echo "Home directory is $HOME" #shows home variable
echo "Currently used terminal is $TERM" #shows terminal information
ls /etc/rc3.d/S* #shows all services started up in runlevel 3 on this system